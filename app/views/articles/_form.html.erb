<%= form_for @article do |l| %>

 <% if @article.errors.any? %>
   <div id="error_explanation">
     <h2>
        <%= pluralize(@article.errors.count, "error") %> prohibited
        this article from being saved:
     </h2>
     <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
     </ul>
    </div>
  <% end %>

 <p>
	<%= l.label :Создатель %><br>
	<%= l.text_field :creator %>
 </p>

 <p>
	<%= l.label :Заголовок %><br>
	<%= l.text_field :title %>
 </p>

 <p>
	<%= l.label :Текст %><br>
	<%= l.text_area :text %>
 </p>

 <p>
	<%= l.label :Заданный_ЧПУ %><br>
	<%= l.text_area :art_url %>
 </p>

 <p>
	<%= l.submit %>
 </p>
<% end %>
