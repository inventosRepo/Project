<div>
	<% if user_signed_in?%>
	<script>
		var name = '<%=current_user.email %>';
		var port = 3010;
		var player = '<%=current_user.playersid %>';
		var player_id_field = '<%=current_user.field_id %>';
		var id_tank = '<%=current_user.id %>';
		var user_id = '<%=current_user.email %>';
	</script>
	<script src="scripts/fullscreen.js"></script>
	<script src="scripts/direction.js"></script>
	<%=current_user.playersid %> <%= link_to "Выйти", destroy_user_session_path, :method => :delete %>
		<div id="keys">
			<div id="left" class="btn_dir"></div>
			<div id="top" class="btn_dir"></div>
			<div id="down" class="btn_dir"></div>
			<div id="right" class="btn_dir"></div>
			<div id="fire" class="btn_dir"></div>
		</div>
		<div onclick="enterFullscreen()" id="btn_fs">На весь экран</div>
		<div id="save" align="center"><button>Сохранить позицию</button></div>
	<% else %>
  <%=form_tag code_hi_path do |f| %>
	   <label for="code">Введите код</label>
	   <%= text_field_tag :code %>
	   <%= submit_tag "Подтвердить" %>
	<% end %>
	<% if flash[:error] %>
	  <div class="notice"><%= flash[:error] %></div>
	<% end %>
	<% end %>
</div>
